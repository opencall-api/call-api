{
  "name": "@opencall-demo/app",
  "version": "1.0.0",
  "module": "src/server.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build src/client/main.ts --outfile public/app.js --minify",
    "start": "bun run src/server.ts",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
